# 06 — 全流程 19 天详细任务与协作表

> 本文档解决"前期详细、后期粗略"的问题：每天每人都有具体任务 + 产出物 + 协作对接点。
> 使用方式：每天开工前看自己当天那一行，收工时确认产出物是否完成。

---

## 阅读导航

| 内容 | 章节 |
|------|------|
| 角色缩写对照 | 第〇节 |
| Phase 1 (Day 1-3) 每日详细任务 | 第一节 |
| Phase 2 (Day 4-12) 每日详细任务 | 第二节 |
| Phase 3 (Day 10-16) 每日详细任务 | 第三节 |
| Phase 4 (Day 17-19) 每日详细任务 | 第四节 |
| 全周期协作依赖关系图 | 第五节 |
| 每日协作关键点速查 | 第六节 |

---

## 〇、角色缩写

| 代号 | 角色 | 核心工作目录 |
|------|------|-------------|
| **A** | 后端工程师 | `backend/` |
| **B** | 前端工程师 | `frontend/` |
| **C** | AI工程师（理解） | `ai/` |
| **D** | AI工程师（生成） | `gen/` |
| **E** | PM/测试/数据 | `docs/` + `knowledge-base/` |

---

## 一、Phase 1：环境搭建与接口定义（Day 1 ~ Day 3）

---

### Day 1 — 项目启动日（全员并行，各自搭建环境）

| 成员 | 今日任务（精确到产出物） | 产出物 | 需要谁配合 | 为谁提供 |
|------|------------------------|--------|-----------|---------|
| **A** | ① `poetry new edu-agent --flat` 初始化 Poetry 项目 ② 按协议文档 1.2 节粘贴全部依赖到 `pyproject.toml` ③ `poetry install` ④ 创建 `.env` 文件（API Key 用占位符） ⑤ 写 `backend/config.py`（Config 类） ⑥ 写 `backend/main.py`（FastAPI + `/health` + CORS） ⑦ 创建 `.gitignore` ⑧ 启动服务 → 浏览器访问 `http://localhost:8000/health` → 截图 | `.env`、`pyproject.toml`、`backend/config.py`、`backend/main.py`、`.gitignore`、`/health` 截图 | 无（独立） | **全队**：`.env` 模板 + Poetry 项目骨架；**C**：填入真实 API Key |
| **B** | ① 确认 Node.js 20 + pnpm 9 ② `pnpm create vite frontend --template vue` ③ `pnpm add vue-router@4 pinia axios element-plus @element-plus/icons-vue` ④ 创建全部目录和空文件（router/views/components/stores/utils/types） ⑤ 配置 `main.js` + `App.vue` + `router/index.js` ⑥ `pnpm dev` → 浏览器白屏无报错 → 截图 ⑦ 收集 3 张参考 UI 截图存入 `docs/ui-reference/` | `frontend/` 项目骨架、`main.js`、`App.vue`、`router/index.js`、参考 UI 截图 | 无（独立） | **E**：确认 Phase 1.2 完成 |
| **C** | ① 注册 DeepSeek 平台 → 申请 API Key → 充值 ¥20 ② 把 Key 发给 A 填入 `.env` ③ 写 `ai/test_llm.py` → 调用 `deepseek-chat` → 看到回复 → 截图 ④ 写 `ai/test_chroma.py` → ChromaDB CRUD 验证 → 截图 ⑤ 创建 `ai/` 全部子目录 ⑥ 下载至少 1 本教材 PDF 放入 `knowledge-base/教材PDF/` | API Key、`test_llm.py` 截图、`test_chroma.py` 截图、1 本教材 PDF | **A**：`.env` 模板（填入 Key） | **D**：教材 PDF 用于 Day 1 PDF 解析测试；**A**：确认 LLM/ChromaDB 可用 |
| **D** | ① `poetry install` 验证全部依赖 import（pptx/docx/fitz/cv2/PIL） ② 创建 `gen/` 全部子目录 ③ 写 `gen/parse/pdf.py`（`extract_pdf_text` 函数） ④ 用 C 找的教材 PDF 测试 → 打印前 300 字 → 截图 ⑤ 写 `gen/test_gen.py` → 手写 3 页测试 PPT（封面/目录/内容）→ 用 PowerPoint 打开确认中文正常 → 截图 | `gen/parse/pdf.py`、`test_gen.py`、PDF 解析截图、测试 PPT 截图 | **C**：教材 PDF 文件 | **C**：`extract_pdf_text` 函数（Day 2 开始可用） |
| **E** | ① 飞书/Notion 建 Sprint Board（按 Master Plan Phase 1 的 8 个交付物建卡片） ② 拉 5 人微信群 → 群公告三条制度 → 确定今晚 21:00 站会 ③ 创建 `knowledge-base/` 全部子目录 ④ 下载至少 2 本教材 PDF + 2 套教案模板 ⑤ 写 `知识库资料清单.xlsx` 初始版 ⑥ 主持 21:00 站会 → 记录"Day 1 完成情况" | Sprint Board 链接、群公告截图、`知识库资料清单.xlsx`、站会纪要 | 无（独立） | **全队**：Sprint Board + 群沟通渠道；**C**：知识库资料 |

**Day 1 协作关键点：**
- A 创建 `.env` → C 填 API Key → A 确认 `poetry install` 全员可用
- C 下载教材 PDF → D 用来测 PDF 解析 → 双方确认 `extract_pdf_text` 函数签名
- E 建 Sprint Board → 全员晚 21:00 站会确认 Day 1 进度

---

### Day 2 — 核心文件创建日（接口合同落地）

| 成员 | 今日任务 | 产出物 | 需要谁配合 | 为谁提供 |
|------|---------|--------|-----------|---------|
| **A** | ① 写 `backend/schemas.py`：按协议文档第 2 节完整抄写全部 Pydantic Model（SessionCreate/SessionInfo/ChatRequest/ChatEvent/FileType/FileInfo/TaskStatus/GenerateRequest/TaskInfo/OutputFile/KnowledgePoint/IntentResult/RAGDocument/ReferenceMaterial/GenerationInstruction） ② 写 `backend/models/session.py`（Session + ChatMessage ORM） ③ 写 `backend/models/file.py`（FileRecord ORM） ④ 写 `backend/models/task.py`（Task ORM） ⑤ 写 `backend/models/__init__.py`（统一导出 + Base + gen_id） ⑥ 运行建表脚本 → `data.db` 生成 → 截图 | `schemas.py`、`models/session.py`、`models/file.py`、`models/task.py`、`models/__init__.py`、`data.db` 截图 | 无（严格按协议文档） | **C + D**：`schemas.py`（今晚必须发给他们，Day 3 他们要 `from backend.schemas import X`） |
| **B** | ① 写 `src/types/index.ts`：按协议文档 2.1 节完整抄 TypeScript 类型定义 ② 写 `src/utils/request.js`（Axios 实例 + baseURL + 拦截器） ③ 写 `src/utils/sse.js`（`createChatStream` 函数：fetch + ReadableStream + SSE 解析） ④ 写 `src/views/Home.vue`（输入框 + 开始设计按钮 + 调 `POST /sessions` 创建会话） ⑤ `pnpm dev` → 看到首页 → 截图 | `types/index.ts`、`utils/request.js`、`utils/sse.js`、`views/Home.vue`、首页截图 | **A**：API 格式（参考协议文档） | **E**：首页 UI 截图 → 确认 Phase 1.2 |
| **C** | ① **写 4 个 Prompt 模板**：`intent_extract.txt` / `follow_up.txt` / `confirm.txt` / `fusion.txt` ② 写 `ai/intent/analyzer.py`（`IntentAnalyzer` 类：`analyze()` + `lock_intent()` + `_gen_follow_up()` + `_gen_confirm()`） ③ 写 `ai/intent/state.py`（会话状态机：init→probing→confirming→locked） ④ 写 `ai/test_intent.py` 自测脚本 → 模拟输入"我想备一节Python列表的课" → 看到追问输出 → 截图 | 4 个 Prompt 模板、`analyzer.py`、`state.py`、`test_intent.py` 截图 | **A**：`schemas.py`（`IntentResult` 等类型） | **A**：`IntentAnalyzer` 类（Day 3 起 A 可以开始对接） |
| **D** | ① 写 `gen/parse/doc.py`（`extract_doc_text`：读取 .docx 文本 + 标题层级） ② 写 `gen/parse/image.py`（`describe_image`：base64 编码图片 → 调 DeepSeek 描述） ③ 写 `gen/parse/video.py`（`extract_keyframes` + `summarize_video`） ④ 用 E 给的教案 Word 测试 `extract_doc_text` → 打印输出 → 截图 ⑤ 用一张测试图片测 `describe_image` → 打印描述 → 截图 | `parse/doc.py`、`parse/image.py`、`parse/video.py`、解析测试截图 | **C**：API Key（`.env`）；**E**：教案 Word 文件 | **C**：`extract_doc_text` / `describe_image` / `summarize_video` 函数 |
| **E** | ① 知识库分类整理（把 Day 1 下载的资料重命名、放入对应子目录） ② 填写 `知识库资料清单.xlsx`（文件名/类型/来源/内容简介/入库状态） ③ 写 `docs/test-plan.md`（6 大模块测试范围 + 策略 + 通过标准） ④ 写 `test-cases.xlsx` 前 25 条（TC01-TC10 对话 + TC11-TC20 文件上传 + TC21-TC25 会话） ⑤ 主持 21:00 站会 → 确认 Day 2 产出物 | `知识库资料清单.xlsx`（更新）、`test-plan.md`、`test-cases.xlsx`（25条）、站会纪要 | **B**：首页截图；**C**：Prompt 模板完成情况 | **全队**：测试计划；**C**：整理好的知识库目录 |

**Day 2 协作关键点：**
- A 写完 `schemas.py` → **当晚必须发给 C 和 D**（他们在 Day 3 要 import）
- D 写完 `parse/doc.py` + `parse/video.py` → C 确认可以 import（签名符合协议文档 4.3 节）
- C 写完 Prompt 模板 → E 检查格式是否规范
- E 整理知识库 → C 确认可以用这些资料测试 RAG

---

### Day 3 — Phase 1 收官日（API 骨架 + 组件静态版 + 验收）

| 成员 | 今日任务 | 产出物 | 需要谁配合 | 为谁提供 |
|------|---------|--------|-----------|---------|
| **A** | ① 写 `backend/routers/session.py`（`POST /sessions` + `GET /sessions/{id}` + `GET /sessions`，Day 3 用内存 dict 代替 DB） ② 写 `backend/routers/file.py`（`POST /files/upload` + `GET /files/{id}`，内存版） ③ 写 `backend/routers/chat.py`（`POST /sessions/{id}/chat`，SSE 骨架，先返回假数据验证格式） ④ 写 `backend/routers/generate.py`（`POST /generate` + `GET /tasks/{id}/status`，内存版） ⑤ 写 `backend/routers/speech.py`（`POST /speech/transcribe`，内存版） ⑥ 写 `backend/routers/download.py`（`GET /download/{file_id}`，骨架） ⑦ 写 `backend/routers/__init__.py` + 在 `main.py` 中注册全部路由 ⑧ 启动服务 → Swagger `http://localhost:8000/docs` 可访问全部端点 → 截图 | 6 个路由文件 + Swagger 截图 | 无（独立，用内存存储） | **B**：Swagger 文档（明天开始对接 API）；**E**：Phase 1.3 验收 |
| **B** | ① 写 `MessageBubble.vue`（接收 props：role/content/event_type，不同颜色气泡） ② 写 `ChatInput.vue`（文字框 + 发送按钮 + emit('send', text)） ③ 写 `QuestionCard.vue`（显示追问选项按钮 + emit('select', option)） ④ 写 `ConfirmPanel.vue`（显示确认总结 + 确认/修改按钮 + emit('confirm')/emit('modify')） ⑤ 写 `Chat.vue`（组合上述 4 个组件 + 用假数据渲染 3 轮对话） ⑥ `pnpm dev` → 看到完整对话页面 → 截图 | `MessageBubble.vue`、`ChatInput.vue`、`QuestionCard.vue`、`ConfirmPanel.vue`、`Chat.vue`、页面截图 | 无（用假数据） | **E**：Phase 1.2 验收 + UI 走查 |
| **C** | ① 写 `ai/rag/loader.py`（`load_documents`：遍历 `knowledge-base/` 下 PDF/txt/docx） ② 写 `ai/rag/splitter.py`（`split_documents`：RecursiveCharacterTextSplitter，chunk_size=500，中文分隔符） ③ 写 `ai/rag/embedder.py`（`build_index`：向量化 → ChromaDB 入库） ④ 写 `ai/rag/retriever.py`（`RAGRetriever` 类：`search()` 方法） ⑤ 写 `ai/build_kb.py`（全流程脚本：load → split → embed） ⑥ 用 E 整理的知识库资料跑一遍入库 → 验证检索 → 截图 | `loader.py`、`splitter.py`、`embedder.py`、`retriever.py`、`build_kb.py`、检索结果截图 | **A**：`schemas.py`（`RAGDocument` 类型）；**D**：`extract_pdf_text` / `extract_doc_text`；**E**：`knowledge-base/` 目录 | **A**：`RAGRetriever` 类；**E**：`build_kb.py`（E 明天自己跑入库） |
| **D** | ① 写 `gen/pptx/layout.py`（全部样式常量：颜色/字体/字号/边距/行距） ② 写 `gen/pptx/slides.py`（4 种页面模板：`add_cover` / `add_toc` / `add_content` / `add_summary`） ③ 用 `slides.py` 的函数改写 `test_gen.py` → 生成新测试 PPT → 检查效果 ④ 截图发给 E | `layout.py`、`slides.py`、测试 PPT 截图 | **A**：`schemas.py`（确认 `GenerationInstruction` 结构） | **C**：确认 PPT 生成的输入格式；**E**：Phase 1.7 验收 |
| **E** | ① 向 C 要 `build_kb.py` → 执行知识库入库 → 做 3 次检索验证并记录结果 ② 补全 `test-cases.xlsx` 到 50+ 条（新增：TC26-TC35 课件生成 / TC36-TC42 导出下载 / TC43-TC50 异常场景） ③ **Phase 1 里程碑验收**：逐一检查 8 个交付物（1.1-1.8）→ 打勾/打叉 → 有未完成的立刻群内通知 ④ 主持 21:00 站会 → 宣布 Phase 1 验收结果 | `test-cases.xlsx`（50+条）、Phase 1 验收报告、站会纪要 | **A**：Swagger 截图；**B**：前端截图；**C**：Chromadb 截图 + RAG 检索截图；**D**：PDF 解析截图 + PPT 截图 | **全队**：Phase 1 是否通过 → 决定明天能否进 Phase 2 |

**Day 3 协作关键点：**
- E 做 Phase 1 验收时如果某项未完成 → 当晚责任人必须补齐 → 第二天正式进入 Phase 2
- A 的 Swagger 必须全部端点可访问 → B 才能开始在 Day 4 对接
- C 的 `build_kb.py` 必须能跑通 → E 才能独立完成知识库入库
- D 的 `slides.py` 模板函数就绪 → 为 Day 4-6 的 PPT generator 做准备

---

## 二、Phase 2：核心逻辑与模型联调（Day 4 ~ Day 12）

---

### Day 4 — 数据库接入 + SSE 对接启动 + PPT 大纲生成

| 成员 | 今日任务 | 产出物 | 需要谁配合 | 为谁提供 |
|------|---------|--------|-----------|---------|
| **A** | ① 写 `backend/services/file_mgr.py`（`save_upload` / `get_file_path` / `list_session_files`） ② 改造 `routers/session.py`：内存 dict → SQLAlchemy Session（读/写 `sessions` 表） ③ 改造 `routers/file.py`：接入 `file_mgr.py` + FileRecord 表 ④ 在 `main.py` 加 `lifespan`：启动时创建 DB engine + 初始化 C/D 模块的单例 ⑤ 跑 `poetry run pytest` → 确保 DB 操作不报错 | `file_mgr.py`、改造后的 `session.py`/`file.py` 路由、`main.py`（lifespan）、测试通过截图 | **C**：`IntentAnalyzer` 类就绪（Day 2 产出） | **B**：Session/File API 可用 |
| **B** | ① 写 `stores/session.js`（Pinia：sessionId/teacherName/subject/messages 列表 + actions） ② 改造 `Home.vue`：对接真实 `POST /sessions` API（替换假数据） ③ 改造 `Chat.vue`：从路由参数读取 sessionId，调 `GET /sessions/{id}` 加载会话信息 ④ 写 `FileUploader.vue` 静态版（拖拽区 + 文件列表 UI，暂不调上传 API） ⑤ 与 A 同步确认：Swagger 上的 Session API 能否正常返回数据 | `stores/session.js`、改造后的 `Home.vue`/`Chat.vue`、`FileUploader.vue` 静态版 | **A**：Session API 可用 | **E**：前后端首次对接验证 |
| **C** | ① 完善 `analyzer.py`：增加对话历史上下文管理（`session_id` → `messages` 映射） ② 写 `ai/intent/state.py` 的完整状态机逻辑（transition：init→probing 当首次分析 / probing→confirming 当 is_complete=True / confirming→locked 当教师确认） ③ 调优 `intent_extract.txt` Prompt：加入 few-shot 示例（至少 3 个不同学科的意图提取例子） ④ 用真实对话测试意图分析 3 轮（模糊输入 → 追问 → 确认）→ 截图 | `analyzer.py`（完善版）、`state.py`（完整状态机）、优化后的 Prompt、3 轮对话测试截图 | **A**：`schemas.py`（`IntentResult` 确认字段完整） | **A**：`analyzer.py` 明天对接 SSE |
| **D** | ① 写 `gen/pptx/generator.py` 的 LLM 大纲生成部分：读取 `ppt_outline.txt` → 调 DeepSeek → 解析 JSON 大纲 ② 写 `gen/prompts/ppt_outline.txt`（输入 GenerationInstruction → 输出每页的 type/title/points 的 JSON） ③ 测试：用假 `GenerationInstruction`（Python 列表课）生成大纲 JSON → 检查格式正确 ④ 写 `gen/prompts/ppt_content.txt`（单页内容展开 Prompt） | `generator.py`（大纲部分）、`ppt_outline.txt`、`ppt_content.txt`、大纲 JSON 测试输出 | **A**：`schemas.py`（`GenerationInstruction` 结构） | 无（D 独立推进，Day 6 给 A） |
| **E** | ① 跑 `poetry run python ai/build_kb.py` 完成知识库入库 → 截图入库结果 ② 做 RAG 检索验证（5 个不同查询 × 每个查 top3）→ 记录检索准确率到表格 ③ **开始对话功能测试**：TC01-TC05（创建会话/获取会话/发送消息/追问响应/确认总结）→ 有问题记录到 `bug-tracker.xlsx` ④ 主持 21:00 站会 | 检索准确率报告、TC01-TC05 测试结果、`bug-tracker.xlsx`（初始）、站会纪要 | **A**：Swagger API；**C**：`build_kb.py` | **A**：API Bug 报告；**C**：RAG 检索反馈 |

**Day 4 协作关键点：**
- A 必须在今天完成 DB 接入 → B 明天才能对接真实的 Session/File API
- C 必须在今天完善 `analyzer.py` → A 明天开始写 SSE 对接代码
- E 今天开始正式测试 → 发现 Bug 立刻群内 @ 对应负责人

---

### Day 5 — 文件上传完整实现 + SSE 对话联调（A ↔ C）

| 成员 | 今日任务 | 产出物 | 需要谁配合 | 为谁提供 |
|------|---------|--------|-----------|---------|
| **A** | ① 完善 `routers/file.py`：文件类型校验（5 种格式白名单）+ 大小限制（视频 500MB/其他 50MB）+ 存储到 `uploads/` ② 完善 `routers/chat.py`：替换假数据 → 真实对接 `IntentAnalyzer.analyze()` → 构造 `ChatEvent` → SSE 流式输出 ③ 写对话历史存取函数：`save_message()` + `load_history()`（操作 `chat_messages` 表） ④ Postman 测试：上传一个 PDF → 调 Chat API → 验证 SSE 返回 `question` 事件 | 完整的 `file.py`/`chat.py` 路由、Postman 测试截图 | **C**：`IntentAnalyzer.analyze()` 可正常调用 | **B**：File Upload API + Chat SSE API 可用 |
| **B** | ① 改造 `FileUploader.vue`：接入真实 `POST /files/upload`（FormData + onUploadProgress 进度条） ② `Chat.vue` 接入真实 SSE：用 `createChatStream` 替换假数据 → 验证文字消息流式追加 ③ 实现打字机效果：收到 `text` 事件 → `requestAnimationFrame` 逐字追加到当前 AI 气泡 ④ 处理 `question` 事件：当收到 `event_type=question` → 渲染 `QuestionCard` ⑤ 处理 `confirm` 事件：当收到 `event_type=confirm` → 渲染 `ConfirmPanel` | `FileUploader.vue`（完整版）、`Chat.vue`（SSE 接入版） | **A**：File Upload API + Chat SSE API 可用 | **E**：文件上传 + 对话功能可测试 |
| **C** | ① 写 `ai/speech/transcriber.py`（`SpeechTranscriber` 类：加载 faster-whisper small 模型 + `transcribe(audio_path)`） ② 测试语音识别：录一段 5 秒中文音频 → 调 `transcribe()` → 检查识别准确率 ③ 配合 A 调试 SSE 对话：A 调 `analyze()` 时如果报错 → 5 分钟内响应修复 ④ 优化 `follow_up.txt` Prompt：确保追问选项合理（2-4 个选项 + allow_free） | `transcriber.py`、语音识别测试截图 | **A**：联调 Chat SSE | **A**：`SpeechTranscriber` 类（Day 7 对接语音 API） |
| **D** | ① 完善 `gen/pptx/generator.py`：串联大纲生成 + slides 模板 → 生成完整 .pptx ② 用假 `GenerationInstruction` 生成 Python 列表课的 10 页 PPT → 用 PowerPoint 打开检查排版 ③ 发现问题修复：中文换行/字体缺失/图片占位/页码 ④ 截图 PPT 关键页面（封面/目录/内容/总结）发群 | `generator.py`（PPT 完整版）、10 页测试 PPT 文件、PPT 截图 | **A**：`schemas.py` | **C**：确认 PPT 生成需要的输入格式 |
| **E** | ① 测试文件上传：TC11-TC15（PDF/Word/PPT/JPG/MP4 各一个 + 格式校验 + 大小限制） ② 测试对话 SSE：TC06-TC10（流式文字/追问卡片/确认面板/异常输入/断网重连） ③ 更新 `bug-tracker.xlsx`：发现的 Bug 标记 P0/P1/P2 优先级 ④ 主持 21:00 站会 → 重点确认 A↔C 对接是否顺利 | TC11-TC15 测试结果、TC06-TC10 测试结果、`bug-tracker.xlsx`（更新）、站会纪要 | **A**：API 可用；**B**：前端可用 | **A/B/C**：Bug 报告 |

**Day 5 协作关键点：**
- **A ↔ C 今天是关键联调日**：A 写 Chat SSE 代码时会频繁调 C 的 `analyze()` → 双方保持在线沟通
- B 今天开始对接真实 API → 发现接口问题立刻 @A
- D 今天产出第一份完整 PPT → E 开始评估 PPT 质量

---

### Day 6 — PPT 生成交付 + 语音 API 接入 + 前端对话完善

| 成员 | 今日任务 | 产出物 | 需要谁配合 | 为谁提供 |
|------|---------|--------|-----------|---------|
| **A** | ① 写 `routers/speech.py`：对接 `SpeechTranscriber.transcribe()` → 接收音频文件 → 返回识别文字 ② 完善 `routers/chat.py`：支持教师确认后的意图锁定 → 调 `intent_analyzer.lock_intent(session_id)` ③ 写 `routers/generate.py` 的骨架：接收 `GenerateRequest` → 创建 Task 记录 → 返回 `task_id` ④ 验证：Postman 调 Speech API → 上传 .wav → 返回文字 | `speech.py`（完整）、`generate.py`（骨架）、Postman 测试截图 | **C**：`SpeechTranscriber` 类 | **B**：Speech API 可用；**B**：Generate API 可用 |
| **B** | ① 写 `VoiceInput.vue`：录音按钮 + `MediaRecorder` + 波形动画 CSS + `emit('transcribed', text)` ② 对接 Speech API：录音 → 转 Blob → FormData → `POST /speech/transcribe` → 填入输入框 ③ 完善 `Chat.vue`：ConfirmPanel 确认后 → 调 `POST /generate` → 跳转 Preview 页 ④ 写 `stores/task.js`（Pinia：taskId/status/progress/outputs + 轮询逻辑） | `VoiceInput.vue`、`stores/task.js`、`Chat.vue`（完善版） | **A**：Speech API + Generate API 可用 | **E**：语音输入 + 课件生成触发可测试 |
| **C** | ① 写 `ai/fusion/fusion.py`（`KnowledgeFuser` 类：`fuse()` 方法 → 意图 + RAG + 参考资料 → GenerationInstruction） ② 测试 fusion：用假 IntentResult + 真实 RAG 检索结果 → 输出 GenerationInstruction → 检查字段完整性 ③ 优化 `fusion.txt` Prompt：加入对参考资料格式的利用逻辑 ④ 跑 `build_kb.py` 更新知识库（加入 E 新收集的资料） | `fusion.py`、fusion 测试输出 | **D**：`extract_pdf_text` 等解析函数（确认 import 可用） | **A**：`KnowledgeFuser` 类（Day 8 orchestrator 用） |
| **D** | ① **交付 `gen_ppt()` 给 A**：确认函数签名 `gen_ppt(instruction: GenerationInstruction, template: str, output_dir: str) -> Path` ② 用 A 的 `schemas.py` 中的 `GenerationInstruction` 做一次端到端测试：构造指令 → 生成 PPT → 确认文件可打开 ③ 开始写 `gen/docx/generator.py`：教案结构（教学目标/重难点/教学过程/方法/活动/作业） ④ 写 `gen/prompts/doc_plan.txt`（教案内容生成 Prompt） | `gen_ppt()` 函数（可交付版）、`docx/generator.py`（骨架）、`doc_plan.txt` | **A**：`schemas.py` | **A**：`gen_ppt` 可 import 使用；**E**：PPT 样例 |
| **E** | ① 测试语音识别：TC16-TC20（正常录音/噪音环境/长句/短句/空录音） ② 用 D 的测试 PPT 做课件质量评估：检查是否含封面+目录+内容+总结 → 记录问题 ③ 给 D 发课件样式反馈（配色/字体/排版建议） ④ 主持 21:00 站会 | TC16-TC20 测试结果、PPT 质量评估、样式反馈 | **B**：语音组件可用；**D**：测试 PPT | **D**：样式反馈 → 优化 layout.py |

**Day 6 协作关键点：**
- **D → A 交付 `gen_ppt()`**：A 确认能 import 且运行不报错
- B 的 `VoiceInput.vue` 需要 A 的 Speech API → 双方确认接口格式
- E 开始做课件质量评估 → 反馈给 D 优化

---

### Day 7 — Word 教案生成 + orchestrator 核心编写 + 前端预览页

| 成员 | 今日任务 | 产出物 | 需要谁配合 | 为谁提供 |
|------|---------|--------|-----------|---------|
| **A** | ① **开始写 `backend/services/orchestrator.py`**（这是整个项目的核心文件）：按协议文档第 5 节的流程 → Step 1-6 逐一实现 ② 今天先完成 Step 1-4（lock_intent → RAG search → parse references → fuse） ③ 写 `routers/generate.py` 的完整逻辑：接收请求 → 创建后台任务 → 调 `orchestrator.run_generation()` → 更新 Task 状态 ④ 实现任务状态轮询：`GET /tasks/{id}/status` 从 DB 读取 Task 记录返回 | `orchestrator.py`（Step 1-4）、`generate.py`（完整版） | **C**：`IntentAnalyzer`/`RAGRetriever`/`KnowledgeFuser`；**D**：解析函数 | **B**：任务轮询 API 可用 |
| **B** | ① 写 `Preview.vue`：轮询 `GET /tasks/{id}/status`（2 秒一次）→ 显示进度条 → completed 后展示课件列表 ② 写 `PPTViewer.vue`：接收 pptx download_url → iframe 或图片预览（先用 Office Online / Google Docs Viewer 嵌入） ③ 写 `DocViewer.vue`：docx 在线预览 ④ 写 `AnimViewer.vue`：iframe 嵌入 HTML5 游戏 ⑤ 写 `EditPanel.vue`：修改意见输入框 + 重新生成按钮 | `Preview.vue`、`PPTViewer.vue`、`DocViewer.vue`、`AnimViewer.vue`、`EditPanel.vue` | **A**：`GET /tasks/{id}/status` 返回格式 | **E**：预览功能可测试 |
| **C** | ① 完善 `fusion.py`：加入参考资料解析逻辑（在 `fuse()` 中调 D 的 `extract_pdf_text` 等） ② 写 `_parse_reference()` 辅助方法：根据 file_type 分发到不同的解析函数 ③ 多轮意图完善：支持教师在 confirm 后说"修改课时为 90 分钟" → analyzer 更新 IntentResult ④ 配合 A 调试 orchestrator Step 1-4：A 调你的函数时如果报错 → 立刻修复 | `fusion.py`（完善版）、`analyzer.py`（支持修改意图） | **D**：所有解析函数（`extract_pdf_text`/`extract_doc_text`/`summarize_video`/`describe_image`） | **A**：`fusion.py` 完整可用 |
| **D** | ① **完善 `gen/docx/generator.py`**：调 LLM 生成教案内容 JSON → 用 python-docx 写 6 大板块 → 样式设置（字体/字号/标题层级） ② 测试：用假指令生成 Python 列表课教案 → Word 打开检查格式 ③ **交付 `gen_doc()` 给 A**：确认函数签名 `gen_doc(instruction: GenerationInstruction, output_dir: str) -> Path` ④ 开始写 `gen/anim/generator.py` 骨架 | `docx/generator.py`（完整版）、`gen_doc()` 可交付、测试教案文件 | **A**：`schemas.py` | **A**：`gen_doc` 可 import；**E**：教案样例 |
| **E** | ① 测试课件生成触发：TC26-TC30（正常生成/无资料生成/超大指令集/并发生成/生成失败重试） ② 给 C 的 RAG 检索做准确率评估（找 10 个查询 → 人工判断 top3 相关性 → 计算 Precision@3） ③ 给 D 的 Word 教案做内容评估（教学目标是否完整/教学过程是否合理） ④ 主持 21:00 站会 → 确认 orchestrator 开发进度 | TC26-TC30 测试结果、RAG Precision@3 报告、教案评估 | **A**：Generate API 可用 | **C**：RAG 反馈；**D**：教案反馈 |

**Day 7 协作关键点：**
- A 写 orchestrator 时会同时调 C 和 D 的函数 → **三方需要保持在线**，有问题立即解决
- D → A 交付 `gen_doc()` → A 明天完成 orchestrator Step 5
- C 确保 `fusion.py` 中调 D 的解析函数不报错

---

### Day 8 — orchestrator 完成 + 动画生成 + 知识融合联调

| 成员 | 今日任务 | 产出物 | 需要谁配合 | 为谁提供 |
|------|---------|--------|-----------|---------|
| **A** | ① **完成 `orchestrator.py` Step 5-6**：调 `gen_ppt()` / `gen_doc()` / `gen_animation()` → 收集输出文件路径 → 注册到 DB → 返回 TaskInfo ② 写 `routers/download.py`：读取文件 → 设置 Content-Type → 返回 `FileResponse` ③ **端到端测试 orchestrator**：创建会话 → 锁定意图 → 运行 `run_generation()` → 检查输出文件 ④ 完善错误处理：整个 pipeline 中任何一步失败 → 更新 Task status=failed + 记录 error 信息 | `orchestrator.py`（完整版）、`download.py`、端到端测试截图 | **C**：全部模块函数；**D**：全部生成函数 | **B**：Download API + 完整的任务系统 |
| **B** | ① 写 `Download.vue`：展示任务完成的课件列表 + 下载按钮（调 `GET /download/{file_id}` + `<a>` 触发下载） ② 完善 `Preview.vue`：对接真实任务数据 + loading/error/empty 三态处理 ③ 错误状态处理：`<el-alert>` 显示生成失败原因 + 重试按钮 ④ 联调全流程：Home → Chat → Upload → Generate → Preview → Download | `Download.vue`、`Preview.vue`（完善版）、全流程联调截图 | **A**：完整的 Generate + Download API | **E**：全链路测试入口 |
| **C** | ① 与 A 联调 orchestrator 全链路：确保 `lock_intent()` → `search()` → `fuse()` 一路不报错 ② 优化 `fusion.txt` Prompt：根据 A 跑出的实际结果调优 → 确保 LLM 输出的 JSON 字段完整 ③ 写 `ai/speech/transcriber.py` 的模型预热逻辑（首次启动时预加载模型） ④ 开始收集常见 edge case：教师输入极短/极长/切换主题/重复确认等 → 更新 Prompt | `fusion.py`（联调优化版）、`transcriber.py`（预热版） | **A**：orchestrator 联调；**D**：解析函数正常 | **A**：稳定版函数供 orchestrator 使用 |
| **D** | ① **写 `gen/anim/generator.py` 完整版**：调 LLM 生成 HTML5 游戏代码 → 保存为 .html ② 写 `gen/prompts/anim_idea.txt`（两种类型 Prompt：quiz_game 知识点闯关 / concept_anim 概念动画） ③ 测试动画生成：用 Python 列表 vs 字典主题 → 生成 HTML → 浏览器打开验证可交互 ④ **交付 `gen_animation()` 给 A** | `anim/generator.py`（完整版）、`anim_idea.txt`、测试 HTML 游戏 | **A**：`schemas.py` | **A**：`gen_animation` 可 import |
| **E** | ① 测试 orchestrator 端到端：按全链路测试脚本完整走一遍（12 步）→ 记录每一步的问题 ② 测下载功能：TC36-TC42（pptx/docx/html 各下载一个 + 文件完整性 + 文件名正确） ③ 整理 P0 Bug 清单 → 群内 @ 对应负责人，设截止日 ④ 主持 21:00 站会 → 确认 orchestrator 是否跑通 | 全链路测试报告、TC36-TC42 测试结果、P0 Bug 清单、站会纪要 | **A**：orchestrator 可用；**B**：前端全流程可用 | **全队**：P0 Bug 分配 |

**Day 8 协作关键点：**
- **A 的 orchestrator 今天必须跑通** → C 和 D 全力配合调试
- B 今天完成前端全流程 → E 明天开始端到端测试
- D 交付最后一个生成函数 `gen_animation()` → A 的 orchestrator 完整

---

### Day 9 — 全链路联调（A ↔ B ↔ C ↔ D 四路并进）

| 成员 | 今日任务 | 产出物 | 需要谁配合 | 为谁提供 |
|------|---------|--------|-----------|---------|
| **A** | ① 修复 orchestrator 端到端测试中发现的问题 ② 完善 API 错误响应：统一错误格式（error_code + detail + timestamp） ③ 写 `tests/test_api.py`：至少 10 个测试用例（session CRUD / file upload / chat SSE / generate / download） ④ `poetry run pytest` → 全部通过 → 截图 ⑤ 导出 Postman Collection JSON → 发给 B 和 E | `test_api.py`（10+ 用例）、Postman Collection、pytest 通过截图 | **C/D**：修复过程中可能需要配合 | **B**：Postman Collection；**E**：测试基准 |
| **B** | ① 全链路 Bug 修复：根据 E 的测试报告修前端 Bug ② 细节打磨：loading 动画/空状态提示/错误重试/键盘快捷键（Enter 发送） ③ 响应式适配：1366×768 最小分辨率 + 1920×1080 最佳效果 ④ 页面过渡动画：路由切换的过渡效果（`<transition>`） | 前端 Bug 修复 + 细节打磨 | **A**：API 修复配合 | **E**：修复后的版本供回归测试 |
| **C** | ① 配合 A 修复 orchestrator 中 intent/rag/fusion 的问题 ② 优化 LLM 调用：加入 retry 机制（失败重试 3 次）+ 超时设置（30s） ③ 写 Prompt 版本管理：在 `ai/prompts/` 下创建 `v1/` 子文件夹，方便后续迭代 ④ 开始收集 Demo 演示对话脚本（准备 2-3 个不同学科的演示场景） | LLM 调用优化、Prompt v1 版本、Demo 对话脚本 | **A**：联调反馈 | **E**：Demo 脚本素材 |
| **D** | ① 修复 PPT/Word/动画生成中的排版问题（根据 E 的反馈） ② 优化 PPT 模板：增加 2 种配色方案（学术蓝 / 活力橙） ③ 测试动画在不同浏览器的兼容性（Chrome / Edge） ④ 准备 3 套样例指令集（入门课/进阶课/复习课）给 E 做 Demo | 优化后的 PPT/Word/动画生成器、3 套样例指令集 | **E**：样式反馈 | **E**：3 套 Demo 课件 |
| **E** | ① 全链路端到端测试（完整 12 步 × 3 轮）→ 记录所有问题 ② 回归测试：修完 Bug 的模块再测一次（TC01-TC42 全覆盖） ③ 整理 Phase 2 进度报告：哪些交付物完成/哪些滞后/需要调整什么 ④ **Day 9 晚里程碑检查**：核心 API 全部完成？课件生成跑通一次？→ 宣布结果 ⑤ 主持 21:00 站会 | 全链路测试报告（3轮）、回归测试结果、Phase 2 进度报告 | **全队**：Bug 修复完成 | **全队**：Phase 2 状态 |

**Day 9 协作关键点：**
- **今天是 Phase 2 内部的集成日**：所有人修复联调问题
- E 做全链路测试 → 发现的问题今天必须修完（明天进入 Phase 3 前端深度接入）
- A 的 Postman Collection 交给 B → B 用来验证前端调用

---

### Day 10 — 前端深度接入 + 动画/游戏完善 + 异常场景覆盖

> ⚠️ Day 10 起 Phase 2 和 Phase 3 并行。A/C/D 继续完善核心逻辑，B 开始前端深度打磨。

| 成员 | 今日任务 | 产出物 | 需要谁配合 | 为谁提供 |
|------|---------|--------|-----------|---------|
| **A** | ① 实现文件上传后的异步解析：上传完成 → 后台调 D 的解析函数 → 解析结果存入 FileRecord ② 增加 API 限流：同一 session 的 generate 请求 30 秒内只能触发一次 ③ 写 API 日志中间件：记录每个请求的 method/path/status/duration ④ 完善 `download.py`：增加文件不存在 404 + 文件过期清理逻辑 | 异步解析、限流中间件、日志中间件、完善后的 download | 无（独立） | **B**：更健壮的 API；**E**：可测试边界情况 |
| **B** | ① **写 `EditPanel.vue` 完整逻辑**：输入修改意见 → `POST /generate`（带修改描述）→ 重新轮询 → 展示新课件 ② 写课件版本历史：展示同一 session 的多次生成记录 ③ 移动端适配：对话页面在手机端的响应式布局（至少保证不崩） ④ 写全局错误处理：Axios 拦截器中统一处理 401/404/500 + Element Plus Message 提示 | `EditPanel.vue`（完整）、版本历史、响应式适配、全局错误处理 | **A**：修改后的 Generate API | **E**：可测试修改反馈流程 |
| **C** | ① 完善语音识别：支持长音频（>30 秒）的分段识别 + 合并结果 ② 写 RAG 混合检索：关键词 BM25 + 向量相似度加权融合 ③ 测试 RAG 检索延迟：100 个查询的平均响应时间 → 目标 < 2 秒 ④ 准备知识库补充材料（与 E 协作：需要什么类型的资料能提升检索效果） | `transcriber.py`（长音频版）、混合检索、RAG 延迟报告 | **E**：知识库资料补充 | **A**：优化后的 retriever |
| **D** | ① 写 PPT 模板系统：支持 `template="academic"` / `template="colorful"` 两种风格 ② 生成多套对比 PPT → 截图发 E 选优 ③ 完善动画生成：增加 `concept_anim` 类型（知识概念的可视化动画，如列表 vs 字典的内存模型对比） ④ 写 `gen/docx/sections.py` 的详细模板（每个板块的默认文本结构和样式） | PPT 双模板、`concept_anim` 类型、`sections.py` | **E**：模板风格选择 | **E**：多风格 Demo 素材 |
| **E** | ① 测试异常场景：TC43-TC50（空输入/超长输入/特殊字符/并发请求/断网/服务重启/文件损坏/不支持的格式） ② 测试修改反馈流程：输入修改意见 → 重新生成 → 验证修改是否生效 ③ 写 Demo 脚本初稿 `docs/demo-script.md`（10 分钟版本，每步精确到秒） ④ 主持 21:00 站会 | TC43-TC50 测试结果、`demo-script.md` 初稿、站会纪要 | **B**：修改反馈功能可用 | **全队**：Demo 脚本供排练 |

**Day 10 协作关键点：**
- B 的修改反馈面板今天完成 → E 马上测试迭代优化闭环
- D 的双模板 PPT → E 今天确定最终风格
- E 的 Demo 脚本初稿 → 全队确认时间线是否合理

---

### Day 11 — 性能优化 + 前端细节打磨 + 知识库扩充

| 成员 | 今日任务 | 产出物 | 需要谁配合 | 为谁提供 |
|------|---------|--------|-----------|---------|
| **A** | ① PPT/Word/动画生成改为后台任务队列（`asyncio.create_task` → 更新 Task progress 百分比） ② 实现生成进度回调：orchestrator 每完成一步 → 更新 Task.progress（20%→40%→60%→80%→100%） ③ 数据库查询优化：给 session_id/file_id/task_id 加索引 ④ 写 `init_db.py` 脚本（一键建库 + 建表）→ 交给 E 归档 | 任务队列、进度回调、索引优化、`init_db.py` | 无（独立优化） | **B**：更精确的进度展示；**E**：部署脚本 |
| **B** | ① 进度条细化：根据 Task.progress 展示分步进度（"正在分析意图…" → "正在检索知识库…" → "正在生成PPT…"） ② PPT 预览优化：用 `officeonline` 或图片切页预览（如果 iframe 方案不稳定则改用图片） ③ 动画预览优化：iframe sandbox 安全策略 + 响应式缩放 ④ 全局 loading/empty/error 三态统一组件 | 分步进度条、PPT/动画预览优化、三态统一 | **A**：Task.progress 字段 | **E**：优化后的前端体验 |
| **C** | ① 知识库扩充：与 E 协作入库 10+ 份新资料（教材章节/教案/试题） ② 重新运行 `build_kb.py` → 验证检索效果是否提升 ③ 优化 Prompt 的 few-shot 示例（根据实际测试中 LLM 的常见错误针对性地加例子） ④ 写 `ai/prompts/v1/` 下的最终版 Prompt → 冻结 v1.0 | 知识库（扩充后）、Prompt v1.0 冻结版 | **E**：新资料 | **A**：更准确的 RAG 结果 |
| **D** | ① PPT 页数控制：根据 `duration_minutes` 自动计算合理页数（每 5 分钟 = 1-2 页内容） ② Word 教案字数控制：根据课时自动调整详略程度 ③ 动画复杂度控制：入门课用 quiz_game、高阶课用 concept_anim ④ 生成 3 套完整 Demo 课件（入门课/进阶课/复习课各一套）→ 发给 E | 页数/字数/复杂度自动适配、3 套 Demo 课件 | **E**：Demo 场景需求 | **E**：Demo 课件材料 |
| **E** | ① 性能测试：记录并填入性能指标表（对话首字响应/PPT 生成/Word 生成/文件上传/语音识别） ② 兼容性测试：Chrome + Edge 最新版各走一遍全流程 ③ 向 C 提供新收集的资料 → 协助入库 ④ 主持 21:00 站会 → 重点确认性能是否达标 | 性能测试报告、兼容性测试结果、知识库扩充 | **C**：资料；**D**：Demo 课件 | **全队**：性能反馈 |

**Day 11 协作关键点：**
- A 的进度回调 → B 的分步进度条 → E 验证进度展示准确
- C + E 知识库扩充 → C 重新入库 → E 验证检索效果提升
- D 的 3 套 Demo 课件 → E 检查是否满足演示需求

---

### Day 12 — Phase 2 收尾 + Phase 3 全面启动

| 成员 | 今日任务 | 产出物 | 需要谁配合 | 为谁提供 |
|------|---------|--------|-----------|---------|
| **A** | ① **Phase 2 最终检查**：全部 10 个交付物（2.1-2.10）逐一确认完成 ② 修复 Phase 2 残留 Bug ③ 整理代码：删除调试 print / 统一 import 顺序 / 补充 docstring ④ 更新 `schemas.py`（如果联调中有字段变更）→ 通知 C/D/B 同步更新 ⑤ 写 `README.md` 的后端部分（如何启动/如何测试） | Phase 2 完成确认、代码整理、`README.md`（后端部分） | **C/D**：确认模块稳定 | **E**：Phase 2 验收 |
| **B** | ① **前端全部页面功能自检**：Home → Chat → Preview → Download 四个页面完整可用 ② 修 E 测出的全部 P0/P1 Bug ③ 前端代码整理：统一组件 props/emits 命名、删除未使用的 import ④ 确保 `pnpm build` 不报错 → 生产构建验证 | 前端自检报告、Bug 修复、`pnpm build` 通过截图 | **A**：全部 API 稳定 | **E**：Phase 3 验收入口 |
| **C** | ① **Phase 2 最终检查**：intent / RAG / fusion / speech 四个模块全部稳定 ② 写 `ai/README.md`（各模块的用途 + 如何单独测试） ③ 整理 Prompt 设计文档（记录每个 Prompt 的设计思路和调优过程）→ 给 E 归档 ④ 确保所有类能被 A 正常 import | Phase 2 完成确认、`ai/README.md`、Prompt 设计文档 | **A**：确认 import 正常 | **E**：归档材料 |
| **D** | ① **Phase 2 最终检查**：PPT / Word / 动画 / 解析四个模块全部稳定 ② 写 `gen/README.md`（各模块用途 + 测试方法） ③ PPT/Word/动画生成各跑 3 次 → 确保稳定性（不能有偶发失败） ④ 确保所有函数能被 A 正常 import | Phase 2 完成确认、`gen/README.md`、稳定性测试报告 | **A**：确认 import 正常 | **E**：归档材料 |
| **E** | ① **Phase 2 里程碑验收**：逐一检查 10 个交付物（2.1-2.10）→ 打勾/打叉 → 未完成的今晚补齐 ② 全部 P0 Bug 必须清零 ③ 更新 Sprint Board → 确认 Phase 3 任务卡片就位 ④ 写 Phase 2 验收报告 → 发群 ⑤ 主持 21:00 站会 → 宣布是否进入 Phase 3 | Phase 2 验收报告、P0 清零确认、Sprint Board 更新、站会纪要 | **全队**：Phase 2 交付物 | **全队**：能否进入 Phase 3 |

**Day 12 协作关键点：**
- **Phase 2 正式截止**：E 逐项验收 → 有任何缺失今晚必须补齐
- A/C/D 各自确认模块 import 正常 → A 做最后一次全链路测试
- B 确保 `pnpm build` 成功 → 为 Day 13 生产部署做准备

---

## 三、Phase 3：前端接入与联调测试（Day 10 ~ Day 16）

> Day 10-12 已在 Phase 2 中覆盖。以下是 Day 13-16 的详细安排。

---

### Day 13 — 全面回归测试 + 性能压测 + UI 走查

| 成员 | 今日任务 | 产出物 | 需要谁配合 | 为谁提供 |
|------|---------|--------|-----------|---------|
| **A** | ① 回归测试配合：E 提的 Bug → 5 分钟内响应 → 30 分钟内修复 ② 性能优化：检查 API 响应时间 → 慢查询优化（加索引/N+1 问题） ③ 增加请求日志持久化（写入 `access.log`） ④ 写后端部署文档（环境变量/依赖安装/启动命令） | Bug 修复、性能优化、`access.log`、部署文档 | **E**：Bug 报告 | **E**：回归测试通过 |
| **B** | ① 全面 UI 走查：与 E 逐页过前端 → 列出 UI 问题清单 → 当天修复 ② 修复移动端布局问题（至少保证 Chrome DevTools 手机模式不崩） ③ 键盘导航：Tab 键切换输入框 / Enter 发送 / Esc 关闭面板 ④ 写前端部署文档（`pnpm build` → `dist/` → Nginx 配置） | UI 修复、键盘导航、前端部署文档 | **E**：UI 问题清单 | **E**：UI 走查通过 |
| **C** | ① 做 RAG 检索压力测试：100 个查询并发 → 记录平均响应时间 ② 优化 ChromaDB 查询（调整 n_results、加过滤条件） ③ 写意图分析的边界测试：10 种边缘输入（空输入/纯表情/英文/中英混/超长输入等） ④ 修复测试中发现的问题 | RAG 压力测试报告、边界测试结果、修复 | **E**：测试用例 | **A**：优化后的模块 |
| **D** | ① 做课件生成稳定性测试：同一指令生成 10 次 → 检查成功率 ② 修复偶发失败（LLM 返回格式异常/文件写入失败等） ③ PPT 文件大小优化：压缩图片/删除冗余 layout ④ 动画兼容性修复：确保在 Chrome/Edge/Safari 都可运行 | 稳定性测试报告、PPT 优化、动画兼容修复 | **E**：测试反馈 | **E**：验收通过 |
| **E** | ① **全量回归测试**：TC01-TC50 全部重跑一遍 → 记录通过率 ② UI 走查：与 B 逐页检查（间距/对齐/颜色/字体/交互反馈）→ 形成 UI 问题清单 ③ 性能指标验收：对照 Phase 3 性能指标表 → 不达标的标注 ④ 主持 21:00 站会 → 确认回归测试结果 | 回归测试报告（50 条）、UI 问题清单、性能验收 | **A/B/C/D**：修复配合 | **全队**：Bug 分配 |

**Day 13 协作关键点：**
- E 主导回归测试 → 发现的问题当天分配给对应负责人
- B 和 E 同步 UI 走查 → B 当天修完 UI 问题
- D 的稳定性测试如果成功率低于 95% → 必须找到根因并修复

---

### Day 14 — 最终 Bug 清零 + 端到端彩排第 1 轮

| 成员 | 今日任务 | 产出物 | 需要谁配合 | 为谁提供 |
|------|---------|--------|-----------|---------|
| **A** | ① **全部 P0/P1 Bug 清零**（这是今天最高优先级） ② 确认全部 API 错误码覆盖（SESSION_NOT_FOUND / FILE_TOO_LARGE / TASK_NOT_FOUND 等） ③ 配合 E 做第 1 轮 Demo 彩排 → 记录后端问题 ④ 代码 freeze：今天之后不再新增功能，只修 Bug | Bug 清零、API 错误码完善、彩排问题记录 | **E**：Bug 清单 + 彩排 | **E**：确认后端就绪 |
| **B** | ① **全部 P0/P1 Bug 清零** ② 配合 E 做第 1 轮 Demo 彩排 → 记录前端问题 ③ 确保 4 个页面在 1920×1080 和 1366×768 分辨率下正常显示 ④ 代码 freeze：冻结 UI 结构和交互 | Bug 清零、多分辨率截图、彩排问题记录 | **E**：Bug 清单 + 彩排 | **E**：确认前端就绪 |
| **C** | ① 配合彩排 → 确保对话流畅自然 → 调整 Prompt 中追问的语气/选项 ② 修复 RAG 检索不准的问题（如果 E 反馈某些查询结果不相关） ③ 准备 Demo 用的预置对话（确保演示时不出意外） ④ 代码 freeze | Prompt 优化、RAG 修复、Demo 预置对话 | **E**：彩排反馈 | **E**：确认 AI 就绪 |
| **D** | ① 配合彩排 → 确保课件生成质量 → 调整模板/样式 ② 确认 3 套 Demo 课件在 PowerPoint/Word/浏览器中正常打开 ③ 修复任何排版/乱码/缺失问题 ④ 代码 freeze | 课件质量优化、Demo 课件确认 | **E**：彩排反馈 | **E**：确认课件就绪 |
| **E** | ① **组织第 1 轮完整 Demo 彩排**：按 `demo-script.md` 严格计时 → 记录每步耗时 + 问题 ② 汇总所有 Bug → 优先级排序 → 分配给对应负责人 ③ **Day 14 晚里程碑检查**：前端全流程可用？P0 Bug 清零？→ 宣布结果 ④ 主持 21:00 站会 → 更新彩排问题清单 | 第 1 轮彩排记录、Bug 清零确认、站会纪要 | **全队**：配合彩排 | **全队**：彩排反馈 |

**Day 14 协作关键点：**
- **代码 freeze**：今天之后所有人不再新增功能
- **第 1 轮彩排**：E 当"教师"完整操作一遍 → 全队观摩 → 记录所有问题
- P0/P1 Bug 必须今天清零（明天只修彩排中发现的问题）

---

### Day 15 — 彩排第 2 轮 + 问题修复

| 成员 | 今日任务 | 产出物 | 需要谁配合 | 为谁提供 |
|------|---------|--------|-----------|---------|
| **A** | ① 修复第 1 轮彩排中发现的全部后端问题 ② 检查全部 API 响应时间 → 确保没有超过 3 秒的接口 ③ 准备演示环境：清理旧数据 / 确认 .env 配置 / 确保服务启动稳定 ④ 参与第 2 轮彩排 | 彩排问题修复、API 响应时间确认、演示环境就绪 | **E**：彩排问题清单 | **E**：确认修复 |
| **B** | ① 修复第 1 轮彩排中发现的全部前端问题 ② 确保所有按钮/链接/交互的 hover/active/loading 状态完整 ③ 确认 `pnpm build` 后的生产版本功能正常 ④ 参与第 2 轮彩排 | 彩排问题修复、交互状态完善、生产版本验证 | **E**：彩排问题清单 | **E**：确认修复 |
| **C** | ① 优化 Demo 预置对话的 Prompt 响应速度（减少不必要的 LLM 调用） ② 准备备用方案：如果 DeepSeek API 在演示时不可用 → 准备离线 fallback 文本 ③ 参与第 2 轮彩排 | 优化响应速度、备用方案 | **E**：彩排问题清单 | **E**：确认修复 |
| **D** | ① 最终确认 3 套 Demo 课件的质量（PPT≥10页/Word≥3页/HTML 可交互） ② 修复第 1 轮彩排中的课件问题（排版/内容/动画） ③ 参与第 2 轮彩排 | 最终版 Demo 课件 | **E**：彩排问题清单 | **E**：确认修复 |
| **E** | ① **组织第 2 轮完整 Demo 彩排**：确认第 1 轮的问题已修复 → 再次计时 → 目标 ≤ 10 分钟 ② 最终确认性能指标全部达标 ③ 更新 `demo-script.md`（根据彩排实际情况调整时间分配和话术） ④ 主持 21:00 站会 → 确认明天最终彩排 | 第 2 轮彩排记录、性能验收确认、`demo-script.md` 最终版 | **全队**：参与彩排 | **全队**：最终反馈 |

**Day 15 协作关键点：**
- 第 2 轮彩排重点：验证第 1 轮问题已修复 + 计时严格控制
- E 更新 Demo 脚本的时间分配 → 全队按新脚本操作

---

### Day 16 — 最终彩排 + Phase 3 验收

| 成员 | 今日任务 | 产出物 | 需要谁配合 | 为谁提供 |
|------|---------|--------|-----------|---------|
| **A** | ① 最后检查：服务启动 / DB 迁移 / 文件目录权限 ② 参与最终彩排 → 确保后端零故障 ③ 归档全部后端代码 → 打 Git tag `v1.0.0` ④ 写后端交接文档（给 E 归档：关键配置/启动步骤/常见问题） | 后端最终确认、Git tag、交接文档 | **E**：最终彩排 | **E**：归档材料 |
| **B** | ① 最后检查：`pnpm build` 成功 / 资源路径正确 / 打包体积合理 ② 参与最终彩排 → 确保前端零卡顿 ③ 归档全部前端代码 → 确认 Git 最新 ④ 写前端交接文档（给 E 归档：构建步骤/Nginx 配置/环境变量） | 前端最终确认、交接文档 | **E**：最终彩排 | **E**：归档材料 |
| **C** | ① 最后检查：AI 模块所有类可正常 import / LLM 调用稳定 ② 参与最终彩排 → 确保对话/检索/融合流畅 ③ 归档全部 AI 代码 + Prompt 模板 ④ 写 AI 模块交接文档 | AI 模块最终确认、交接文档 | **E**：最终彩排 | **E**：归档材料 |
| **D** | ① 最后检查：课件生成函数稳定 / 解析函数正常 ② 参与最终彩排 → 确保课件质量 ③ 归档全部 gen 代码 + 3 套 Demo 课件 ④ 写课件生成模块交接文档 | 课件模块最终确认、交接文档 | **E**：最终彩排 | **E**：归档材料 |
| **E** | ① **组织最终彩排**：按比赛标准全程模拟 → 计时 → 打分 ② **Phase 3 里程碑验收**：全部 7 个交付物（3.1-3.7）逐一确认 ③ 确认所有归档材料齐全 ④ 主持 21:00 站会 → 宣布进入 Phase 4 | 最终彩排记录、Phase 3 验收报告 | **全队**：参与彩排 | **全队**：Phase 3 通过确认 |

**Day 16 协作关键点：**
- **最终彩排**：模拟比赛现场 → 所有人不准打断 → E 严格计时和评分
- 代码全部打 Git tag → 为 Phase 4 文档撰写锁定版本
- 全队确认可以进入 Phase 4

---

## 四、Phase 4：文档与交付（Day 17 ~ Day 19）

---

### Day 17 — 竞赛文档集中撰写日

| 成员 | 今日任务 | 产出物 | 需要谁配合 | 为谁提供 |
|------|---------|--------|-----------|---------|
| **A** | ① 为 E 提供"系统架构设计"章节的技术内容（后端架构图 + API 设计说明 + 数据库设计要点） ② 提供部署说明（环境要求/启动步骤/配置说明） ③ 提供创新点中的技术细节（如实时 SSE 流式对话的技术亮点） ④ 审查 E 写的技术章节是否有误 | 架构设计文档素材、部署说明、创新点素材 | **E**：文档框架 | **E**：文档内容 |
| **B** | ① 为 E 提供前端截图（4 个页面 × 2 种状态 = 8 张高清截图） ② 提供前端技术亮点说明（打字机效果/SSE 流式解析/响应式布局等） ③ 审查 E 写的文档中前端相关内容 | 页面截图（8 张）、前端技术亮点 | **E**：文档框架 | **E**：文档内容 |
| **C** | ① 为 E 提供 RAG 流程图 + 意图理解流程图（可用 draw.io 或 Mermaid 画） ② 提供 Prompt 设计理念说明（为什么这样设计追问/确认流程） ③ 审查 E 写的 AI 技术章节 | RAG 流程图、意图理解流程图、Prompt 设计文档 | **E**：文档框架 | **E**：文档内容 |
| **D** | ① 为 E 提供课件生成流程图 + 多模态解析流程图 ② 提供 PPT/Word 生成的样例截图（高质量 PNG） ③ 审查 E 写的课件生成技术章节 | 课件生成流程图、样例截图 | **E**：文档框架 | **E**：文档内容 |
| **E** | ① **写项目概要介绍**（1500-2000 字）：背景→方案→功能→创新→场景 ② **写项目详细方案**（3000-5000 字）：需求→架构→数据库→接口→关键技术→测试→部署 ③ 收集全队提供的素材并整合 ④ 主持 21:00 站会 → 确认文档进度 | 项目概要介绍 v1、项目详细方案 v1 | **A/B/C/D**：素材 | **全队**：审阅 |

**Day 17 协作关键点：**
- E 主笔文档 → 其他人提供技术素材 → 当天 E 完成初稿
- 所有人审查自己领域的章节是否正确

---

### Day 18 — 文档定稿 + 演示视频录制 + 项目简介 PPT

| 成员 | 今日任务 | 产出物 | 需要谁配合 | 为谁提供 |
|------|---------|--------|-----------|---------|
| **A** | ① 审查项目方案文档最终版（重点关注技术架构/API/部署章节） ② 配合视频录制（可能需要录一段后端启动/终端操作的画面） ③ 确保演示环境干净（清理测试数据 → 初始化一个干净的 Demo 环境） | 文档审查通过、演示环境就绪 | **E**：文档终稿 | **E**：最终版确认 |
| **B** | ① 审查项目方案文档最终版（重点关注前端/交互/UI 章节） ② 配合视频录制（前端操作录屏） ③ 确认演示用浏览器干净（无插件弹窗/无多余标签页） | 文档审查通过、演示浏览器就绪 | **E**：文档终稿 | **E**：最终版确认 |
| **C** | ① 审查项目方案文档最终版（重点关注 AI/意图/RAG 章节） ② 配合视频录制（AI 响应部分） ③ 确认 LLM API Key 余额充足 → 不够立刻充值 | 文档审查通过、API 余额确认 | **E**：文档终稿 | **E**：最终版确认 |
| **D** | ① 审查项目方案文档最终版（重点关注课件生成/多模态解析章节） ② 配合视频录制（课件生成结果展示） ③ 最终确认 3 套 Demo 课件质量 | 文档审查通过、Demo 课件确认 | **E**：文档终稿 | **E**：最终版确认 |
| **E** | ① 完成项目方案文档修改 → 定稿 ② **录制演示视频**（OBS 录屏 → 10 分钟内）：0-2min 项目介绍 + 2-8min Demo 操作 + 8-10min 创新总结 ③ 用剪映剪辑视频（加字幕/转场/背景音） ④ **做项目简介 PPT**（15 页）：按协议文档 4.2 节的页面规划 | 文档终稿、演示视频（初版）、项目简介 PPT | **全队**：审查反馈 + 配合录制 | **全队**：最终审查 |

**Day 18 协作关键点：**
- E 主录视频 → 全队配合（确保系统在这段时间稳定运行）
- 文档在今天必须定稿（Day 19 只做最终检查和打包）

---

### Day 19 — 最终检查 + 打包交付

| 成员 | 今日任务 | 产出物 | 需要谁配合 | 为谁提供 |
|------|---------|--------|-----------|---------|
| **A** | ① 最终检查：代码全部 push / 无未提交文件 / Git tag 正确 ② 确认所有依赖版本锁定（`poetry.lock` 存在） ③ 配合 E 做最终打包（代码 + 文档 + 视频 + PPT） ④ 参与最终评审会 → 确认交付完整 | 代码最终确认、参与评审 | **E**：打包需求 | **E**：代码归档 |
| **B** | ① 最终检查：`pnpm build` 产物正确 / 前端代码 push / 依赖锁定（`pnpm-lock.yaml`） ② 配合 E 做最终打包 ③ 参与最终评审会 | 前端最终确认、参与评审 | **E**：打包需求 | **E**：代码归档 |
| **C** | ① 最终检查：AI 模块代码全部 push / Prompt 模板完整 ② 确认 ChromaDB 数据可复现（`build_kb.py` 能重建知识库） ③ 参与最终评审会 | AI 模块最终确认、知识库可复现确认 | **E**：打包需求 | **E**：代码归档 |
| **D** | ① 最终检查：gen 模块代码全部 push / 3 套 Demo 课件归档 ② 确认生成环境依赖完整（FFmpeg 路径等注意事项写在 README） ③ 参与最终评审会 | gen 模块最终确认、Demo 课件归档 | **E**：打包需求 | **E**：代码归档 |
| **E** | ① **最终打包**：项目根目录打包为 `.zip`（含全部代码/文档/视频/PPT/知识库/Chromadb 数据） ② 最终检查清单逐项确认（见下方） ③ 组织最终评审会：全队一起过一遍交付物 → 确认完整 ④ 提交竞赛材料 | 最终交付包 `.zip`、最终检查清单、评审会纪要 | **全队**：参与评审 | 竞赛组委会 |

**Day 19 最终检查清单：**

| # | 检查项 | 负责人 | 状态 |
|---|--------|--------|------|
| 1 | `poetry install` → `python backend/main.py` 不报错 | A | □ |
| 2 | `pnpm install` → `pnpm dev` 前端可启动 | B | □ |
| 3 | `poetry run python ai/build_kb.py` 可重建知识库 | C | □ |
| 4 | `poetry run python gen/test_gen.py` PPT 可生成 | D | □ |
| 5 | 全流程 12 步 Demo 走通 | E + 全队 | □ |
| 6 | 项目概要介绍 Word 定稿 | E | □ |
| 7 | 项目详细方案 Word 定稿 | E | □ |
| 8 | 演示视频 MP4（≤ 10 分钟） | E | □ |
| 9 | 项目简介 PPT（≤ 15 页） | E | □ |
| 10 | Git 仓库最终 push（dev + main） | A | □ |

**Day 19 协作关键点：**
- 全队最后聚在一起 → E 逐项过检查清单 → 每项确认打勾
- 打包 `.zip` → E 在另一台电脑解压测试 → 确认能跑通
- 提交前所有人最后看一眼自己的代码和文档

---

## 五、全周期协作依赖关系图

```
Day 1   全员并行搭建环境
        A ──→ 全队(.env + Poetry)
        C ──→ D(教材PDF测试)
        E ──→ 全队(Sprint Board)

Day 2   A ──→ C,D(schemas.py)
        D ──→ C(解析函数)
        C ──→ A(analyzer.py 类)
        E ──→ C(知识库目录)

Day 3   A ──→ B(Swagger 文档)
        C ──→ A(RAGRetriever), E(build_kb.py)
        D ──→ 自闭环(PPT模板)
        E ──→ 全队(Phase 1 验收)

Day 4   A ←── C(IntentAnalyzer 对接)
        B ←── A(Session/File API)
        E ←── C(build_kb.py 入库)

Day 5   A ←── C(analyzer 联调) ← 关键日
        B ←── A(Chat SSE + File Upload API)
        D ──→ E(PPT 样例评估)

Day 6   D ──→ A(gen_ppt 交付) ← 交付节点
        A ←── C(SpeechTranscriber 对接)
        B ←── A(Speech API)
        C ──→ A(KnowledgeFuser 类)

Day 7   A ←── C(fusion 联调) + D(解析函数) ← 三方联调
        D ──→ A(gen_doc 交付)
        B ←── A(Generate + Task API)

Day 8   A ←── C + D(orchestrator 全链路) ← 最关键的联调日
        D ──→ A(gen_animation 交付)
        B ←── A(Download API)

Day 9   A + B + C + D 四路并进修 Bug
        E 全链路测试 3 轮

Day 10  B ──→ E(修改反馈面板)
        D ──→ E(双模板 PPT)
        C + E ──→ 知识库扩充

Day 11  A ──→ B(进度回调细化)
        C + E ──→ 知识库入库验证
        D ──→ E(3套 Demo 课件)

Day 12  E ──→ 全队(Phase 2 验收) ← 里程碑

Day 13  E ──→ 全队(回归测试 + UI 走查)

Day 14  E ──→ 全队(第1轮彩排) ← 代码 freeze

Day 15  E ──→ 全队(第2轮彩排)

Day 16  E ──→ 全队(最终彩排 + Phase 3 验收) ← 里程碑

Day 17  A+B+C+D ──→ E(文档素材)

Day 18  E ──→ 全队(文档终稿审阅)
        全队 ──→ E(配合视频录制)

Day 19  E ──→ 全队(最终打包 + 评审) ← 交付
```

---

## 六、每日协作关键点速查（给 PM 用）

| 日期 | 最关键的协作事项 | 风险点 | 必须确认 |
|------|-----------------|--------|---------|
| Day 1 | C 申请 API Key → A 填 .env | API Key 申请被拒 | C 的 test_llm.py 截图 |
| Day 2 | A 发 schemas.py → C/D | schemas 不完整 | C/D 确认能 import |
| Day 3 | E 做 Phase 1 验收 | 某人未完成 | 8 项全部打勾 |
| Day 4 | A DB 接入 + C analyzer 完善 | DB 建表失败 | `/docs` 可访问全部端点 |
| Day 5 | **A ↔ C 联调 Chat SSE** | analyzer 返回格式不对 | Postman SSE 返回正确 |
| Day 6 | **D → A 交付 gen_ppt** | 函数签名不匹配 | A 能 import 并运行 |
| Day 7 | **A ↔ C ↔ D 三方联调 orchestrator** | import 报错 | Step 1-4 跑通 |
| Day 8 | **orchestrator 全链路跑通** | 某一步卡住 | 端到端生成 .pptx |
| Day 9 | 四路修 Bug | P0 Bug 太多 | 全链路测试 3 轮 |
| Day 10 | B 修改反馈面板 + D 双模板 | 新功能引入新 Bug | E 测试通过 |
| Day 11 | A 进度回调 + D Demo 课件 | 进度展示不准 | 进度条能与实际同步 |
| Day 12 | **Phase 2 验收** | 有交付物未完成 | 10 项全部打勾 |
| Day 13 | 回归测试 + UI 走查 | UI 问题太多 | 50 条用例通过率 |
| Day 14 | **第 1 轮彩排 + 代码 freeze** | 彩排中断 | 12 步全流程 ≤ 15 分钟 |
| Day 15 | 第 2 轮彩排 | 前一问题未修复 | ≤ 10 分钟 |
| Day 16 | **最终彩排 + Phase 3 验收** | 任何故障 | 7 项全部打勾 |
| Day 17 | 写竞赛文档 | 素材不够 | 初稿完成 |
| Day 18 | 录视频 + 做 PPT | 系统不稳定 | 视频录制成功 |
| Day 19 | **最终打包交付** | 打包缺文件 | 检查清单 10 项全打勾 |

---

*本文档由 PM（成员E）维护。每天站会时对照"今日任务"列逐人确认进度。*
*版本 v1.0 | 2026-07-17*

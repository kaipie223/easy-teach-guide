# 姜文杰 · 后端工程师 — 模块视角

> **你的定位：** 你是整个系统的骨架。每个功能模块的 API 都由你提供，模块间的调度由你编排。
> 本文档按 7 个功能模块列出你需要做的事情——不再只是"写后端代码"，而是**确保你参与的模块端到端跑通**。

---

## 你在每个模块中的角色

| 模块 | 你的角色 | 关键度 | 目标日期 |
|------|---------|--------|---------|
| M1 会话与对话引导 | Session API + Chat SSE API，import 陈澜的 IntentAnalyzer | ⭐⭐⭐ | 8/2 |
| M2 文件上传与解析 | Upload API + 文件管理，后台调赵钰洁的解析函数 | ⭐⭐⭐ | 8/1 |
| M3 知识库与RAG | 无需直接参与（陈澜和陶克钦负责） | ⭐ | — |
| M4 语音输入 | Speech API，import 陈澜的 SpeechTranscriber | ⭐⭐ | 8/2 |
| M5 课件智能生成 | **核心**：orchestrator.py 编排全流程 + Generate/Task API | ⭐⭐⭐⭐⭐ | 8/12 |
| M6 预览反馈 | Task 轮询 API（M5 已有） | ⭐⭐ | 8/16 |
| M7 导出下载 | Download API（FileResponse） | ⭐⭐ | 8/18 |

---

## M1 — 会话与对话引导

### 你需要交付的

| # | 交付物 | 文件 | 验收 |
|---|--------|------|------|
| 1 | Session CRUD API | `backend/routers/session.py` | POST/GET 全部正常 |
| 2 | Session + ChatMessage ORM | `backend/models/session.py` | data.db 建表成功 |
| 3 | Chat SSE API | `backend/routers/chat.py` | SSE 流式返回 question/confirm 事件 |
| 4 | 在 lifespan 中初始化 IntentAnalyzer 单例 | `backend/main.py` | `from ai.intent.analyzer import IntentAnalyzer` 不报错 |

### 关键对接

```
你的 chat.py → import 陈澜的 IntentAnalyzer → analyzer.analyze(session_id, messages) → IntentResult
你的 chat.py → 根据 is_complete 决定发 question 还是 confirm 事件 → SSE 推给潘卓然
```

### Done 标准
- [ ] `POST /api/v1/sessions` 创建会话 → 返回 session_id
- [ ] `POST /api/v1/sessions/{id}/chat` → SSE 流正确推送 ChatEvent
- [ ] 陈澜的 `IntentAnalyzer.analyze()` 在你的环境正常 import 且不报错

---

## M2 — 文件上传与多模态解析

### 你需要交付的

| # | 交付物 | 文件 | 验收 |
|---|--------|------|------|
| 1 | FileRecord ORM | `backend/models/file.py` | data.db 建表成功 |
| 2 | 文件上传 API（校验 + 存储） | `backend/routers/file.py` | 5 种格式各上传一个成功 |
| 3 | 文件管理服务 | `backend/services/file_mgr.py` | save/read/list 三个方法可用 |
| 4 | 后台异步解析（可选，8/10 前） | `services/file_mgr.py` | 上传后自动调赵钰洁的解析函数 |

### 关键对接

```
你的 file.py → 接收 multipart → 校验类型/大小 → 保存到 uploads/ → 写 DB → 返回 FileInfo
```

### Done 标准
- [ ] PDF/Word/PPT/Image/Video 各上传一个全部成功
- [ ] 不支持格式返回 422，超大文件返回 413
- [ ] 赵钰洁的解析函数能在你的环境正常 import

---

## M4 — 语音输入

### 你需要交付的

| # | 交付物 | 文件 | 验收 |
|---|--------|------|------|
| 1 | Speech API | `backend/routers/speech.py` | 上传音频 → 返回 `{"text": "..."}` |

### 关键对接

```
你的 speech.py → 接收音频文件 → 保存临时文件 → import 陈澜的 SpeechTranscriber
→ transcriber.transcribe(temp_path) → 返回文字 → 删除临时文件
```

### Done 标准
- [ ] Postman 上传 .wav → 返回正确的中文文字
- [ ] 陈澜的 `SpeechTranscriber.transcribe()` 在你的环境正常 import

---

## M5 — 课件智能生成（你最核心的工作）

### 你需要交付的

| # | 交付物 | 文件 | 验收 |
|---|--------|------|------|
| 1 | Task ORM | `backend/models/task.py` | data.db 建表成功 |
| 2 | Generate API（触发 + 轮询） | `backend/routers/generate.py` | POST → task_id → GET 轮询 → completed |
| 3 | **Orchestrator（核心编排器）** | `backend/services/orchestrator.py` | run_generation() 全流程不报错 |

### orchestrator.py 的 6 步调度（最重要！）

```python
def run_generation(session_id: str) -> TaskInfo:
    # Step 1: 锁定意图（陈澜）
    intent = self.intent_analyzer.lock_intent(session_id)

    # Step 2: RAG 检索（陈澜）
    query = f"{intent.teaching_goal} {所有知识点title}"
    rag_docs = self.rag_retriever.search(query, top_k=5)

    # Step 3: 解析参考资料（赵钰洁）
    references = []
    for file in session_files(session_id):
        text = 根据 file_type 调赵钰洁的解析函数
        references.append(ReferenceMaterial(...))

    # Step 4: 知识融合（陈澜）
    instruction = self.fuser.fuse(intent, rag_docs, references)

    # Step 5: 课件生成（赵钰洁）
    pptx_path = gen_ppt(instruction, output_dir=...)
    docx_path = gen_doc(instruction, output_dir=...)
    html_path = gen_animation(instruction, output_dir=...)

    # Step 6: 注册输出 → 更新 Task 为 completed
    return TaskInfo(outputs=[...], status="completed")
```

### 你需要 import 的全部模块

```python
from ai.intent.analyzer import IntentAnalyzer      # 陈澜提供
from ai.rag.retriever import RAGRetriever           # 陈澜提供
from ai.fusion.fusion import KnowledgeFuser          # 陈澜提供
from gen.pptx.generator import gen_ppt               # 赵钰洁提供
from gen.docx.generator import gen_doc               # 赵钰洁提供
from gen.anim.generator import gen_animation         # 赵钰洁提供
from gen.parse.pdf import extract_pdf_text           # 赵钰洁提供
from gen.parse.doc import extract_doc_text           # 赵钰洁提供
from gen.parse.video import summarize_video          # 赵钰洁提供
```

### Done 标准（目标：8/12）
- [ ] `run_generation(session_id)` 完整链路跑通，不报错
- [ ] output/ 目录下有 .pptx / .docx / .html 三个文件
- [ ] Task 状态从 pending → processing → completed
- [ ] 进度百分比正确更新（20% → 40% → 60% → 80% → 100%）

---

## M7 — 导出下载

### 你需要交付的

| # | 交付物 | 文件 | 验收 |
|---|--------|------|------|
| 1 | Download API | `backend/routers/download.py` | GET /download/{file_id} → 浏览器触发下载 |

### Done 标准
- [ ] 下载 .pptx → PowerPoint 可打开
- [ ] 下载 .docx → Word 可打开
- [ ] 下载 .html → 浏览器正常显示
- [ ] 不存在的 file_id → 返回 404

---

## 你的开发时间线

```
7/22-7/24:  环境搭建 + schemas.py + 所有路由骨架
            产出：.env / pyproject.toml / schemas.py / 6个路由文件 / ORM模型

7/25-7/29:  M1 对话 API + M4 语音 API
            对接陈澜的 IntentAnalyzer 和 SpeechTranscriber

7/30-8/5:   M5 orchestrator.py（最核心！）
            对接陈澜的 RAGRetriever + KnowledgeFuser
            对接赵钰洁的 gen_ppt / gen_doc / gen_animation

8/6-8/10:   M2 文件上传完善 + M7 下载 + 错误处理
            异常处理 + 测试 + Postman Collection

8/11-8/22:  全链路联调 + Bug 修复 + 配合陶克钦验收 + 彩排
```

---

## 你需要从每个人那里拿到什么

| 从谁 | 需要什么 | 用到哪个模块 | 何时需要 |
|------|---------|-------------|---------|
| 陈澜 | `IntentAnalyzer` 类 | M1 | 7/28 |
| 陈澜 | `RAGRetriever` 类 | M5 | 8/3 |
| 陈澜 | `KnowledgeFuser` 类 | M5 | 8/8 |
| 陈澜 | `SpeechTranscriber` 类 | M4 | 7/30 |
| 赵钰洁 | `gen_ppt()` | M5 | 8/8 |
| 赵钰洁 | `gen_doc()` | M5 | 8/8 |
| 赵钰洁 | `gen_animation()` | M5 | 8/10 |
| 赵钰洁 | `extract_pdf_text()` 等解析函数 | M5 | 8/1 |
| 陶克钦 | `.env` 中的真实 API Key | 全部 | 7/22 |

# 01 — 成员A · 后端工程师

> **你是系统的骨架。所有模块通过你的编排串联在一起。**
> 核心职责边界：**前端发来的请求 → 正确调度 AI 模块 → 把结果返回前端。整个管道不中断。**

---

## 你需要产出的文件（完整清单）

```
项目根目录/
├── .env                              # 全局环境变量，你创建并分发给全队
├── .gitignore                        # 你创建
├── pyproject.toml                    # Poetry 配置，你创建
├── backend/
│   ├── main.py                       # FastAPI 入口 + 生命周期管理
│   ├── config.py                     # 全局配置类（读 .env）
│   ├── schemas.py                    # 所有 Pydantic 模型（协议文档第2节）
│   ├── models/
│   │   ├── __init__.py
│   │   ├── session.py                # Session 表 + ChatMessage 表
│   │   ├── file.py                   # FileRecord 表
│   │   └── task.py                   # Task 表
│   ├── routers/
│   │   ├── __init__.py
│   │   ├── session.py                # POST/GET /sessions
│   │   ├── chat.py                   # POST /sessions/{id}/chat (SSE)
│   │   ├── file.py                   # POST/GET /files
│   │   ├── generate.py               # POST /generate + GET /tasks/{id}/status
│   │   ├── speech.py                 # POST /speech/transcribe
│   │   └── download.py               # GET /download/{file_id}
│   ├── services/
│   │   ├── __init__.py
│   │   ├── orchestrator.py           # run_generation() — 核心调度（协议文档第5节）
│   │   └── file_mgr.py               # 文件存储/读取
│   ├── uploads/                      # 用户上传目录（.gitignore）
│   └── __init__.py
└── tests/
    ├── __init__.py
    └── test_api.py                   # API 基础测试
```

---

## 你的技术清单

| 需求 | 精确安装命令 |
|------|-------------|
| Python 3.11.9 | 从 python.org 下载安装 |
| Poetry | `pip install poetry==1.8.3` |
| 创建项目 | `poetry new edu-agent --flat`（在项目根目录执行） |
| 依赖安装 | 把协议文档 1.2 节的依赖清单写入 `pyproject.toml`，然后 `poetry install` |
| IDE | PyCharm Professional → 安装 Pydantic Plugin |

---

## Phase 1：环境搭建（Day 1-3）— 精确操作步骤

### Day 1 — 今天必须完成

**任务 1.1：初始化 Python 项目（30 分钟）**

```bash
# 1. 确认 Python 版本
python --version    # 必须输出 3.11.x

# 2. 在项目根目录初始化 Poetry 项目
cd "C:\Users\kaipie\Desktop\省服务外包大赛\多模态AI教学系统"
poetry new edu-agent --flat --name backend
# --flat 表示不创建子目录，直接用当前目录

# 3. 编辑 pyproject.toml，粘贴协议文档 1.2 节的所有依赖
# 然后安装
poetry install

# 4. 验证
poetry run python -c "import fastapi; print('FastAPI', fastapi.__version__)"
poetry run python -c "import sqlalchemy; print('SQLAlchemy', sqlalchemy.__version__)"
poetry run python -c "import pydantic; print('Pydantic', pydantic.__version__)"
```

**任务 1.2：创建 `.env` + `config.py`（15 分钟）**

```bash
# .env — 项目根目录
touch .env
```

```python
# backend/config.py — 贴这个文件，不要自己改结构
import os
from dotenv import load_dotenv

load_dotenv()

class Config:
    DEEPSEEK_API_KEY = os.getenv("DEEPSEEK_API_KEY", "sk-placeholder")
    DEEPSEEK_BASE_URL = os.getenv("DEEPSEEK_BASE_URL", "https://api.deepseek.com")
    WHISPER_MODEL = os.getenv("WHISPER_MODEL", "small")
    CHROMA_PERSIST_DIR = os.getenv("CHROMA_PERSIST_DIR", "./chroma_data")
    DATABASE_URL = os.getenv("DATABASE_URL", "sqlite:///./data.db")
    UPLOAD_DIR = os.getenv("UPLOAD_DIR", "./backend/uploads")
    OUTPUT_DIR = os.getenv("OUTPUT_DIR", "./output")

config = Config()
```

**任务 1.3：写 `main.py` + 跑通 `/health`（15 分钟）**

```python
# backend/main.py
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
import uvicorn

app = FastAPI(title="EduAgent API", version="0.1.0")

app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173"],   # B 的 Vite 开发端口
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

@app.get("/health")
def health():
    return {"status": "ok", "version": "0.1.0"}

if __name__ == "__main__":
    uvicorn.run("backend.main:app", host="0.0.0.0", port=8000, reload=True)
```

```bash
# 启动
poetry run python -m backend.main
# 浏览器打开 http://localhost:8000/health → 看到 {"status":"ok","version":"0.1.0"}
# 浏览器打开 http://localhost:8000/docs → 看到 Swagger 页面

# 截图发群
```

**任务 1.4：创建 `.gitignore`（5 分钟）**

```gitignore
# .gitignore
__pycache__/
*.pyc
.env
*.db
backend/uploads/*
!backend/uploads/.gitkeep
output/*
!output/.gitkeep
chroma_data/
node_modules/
dist/
.vscode/
.idea/
*.log
```

---

### Day 2 — 数据库 + schemas.py

**任务 2.1：创建 `schemas.py`（按协议文档第 2 节完整抄写）**

```python
# backend/schemas.py
# 严格按 docs/01_技术协议与接口规范_Technical_Protocol.md 第 2 节的定义
# 复制所有 Pydantic Model，一个不缺：
# SessionCreate, SessionInfo, ChatRequest, ChatEvent,
# FileType(enum), FileInfo, TaskStatus(enum), GenerateRequest,
# TaskInfo, OutputFile, KnowledgePoint, IntentResult,
# RAGDocument, ReferenceMaterial, GenerationInstruction
```

> **这一步极其重要。** 这个文件是 A、C、D 共享的数据合同。你写完发群里，C 和 D 各自 `from backend.schemas import X`。

**任务 2.2：建 ORM 模型 + 初始化数据库**

```python
# backend/models/session.py
from sqlalchemy import Column, String, DateTime, Text, create_engine
from sqlalchemy.orm import declarative_base, sessionmaker
from datetime import datetime, timezone
import uuid

Base = declarative_base()

def gen_id(prefix: str) -> str:
    return f"{prefix}_{uuid.uuid4().hex[:8]}"

class Session(Base):
    __tablename__ = "sessions"
    session_id = Column(String, primary_key=True, default=lambda: gen_id("s"))
    teacher_name = Column(String, default="")
    subject = Column(String, default="")
    status = Column(String, default="active")   # active | completed
    created_at = Column(DateTime, default=lambda: datetime.now(timezone.utc))

class ChatMessage(Base):
    __tablename__ = "chat_messages"
    id = Column(String, primary_key=True, default=lambda: gen_id("msg"))
    session_id = Column(String, nullable=False)
    role = Column(String, nullable=False)        # user | assistant
    content = Column(Text, nullable=False)
    msg_type = Column(String, default="text")    # text | question | confirm
    created_at = Column(DateTime, default=lambda: datetime.now(timezone.utc))
```

```python
# backend/models/file.py
from .session import Base, gen_id
from sqlalchemy import Column, String, Float, DateTime
from datetime import datetime, timezone

class FileRecord(Base):
    __tablename__ = "files"
    file_id = Column(String, primary_key=True, default=lambda: gen_id("f"))
    session_id = Column(String, nullable=False)
    original_name = Column(String, nullable=False)
    file_type = Column(String, nullable=False)   # pdf | word | ppt | image | video
    stored_path = Column(String, nullable=False)  # 服务器上的路径
    size_kb = Column(Float, default=0)
    ref_description = Column(String, nullable=True)
    upload_time = Column(DateTime, default=lambda: datetime.now(timezone.utc))
```

```python
# backend/models/task.py
from .session import Base, gen_id
from sqlalchemy import Column, String, Integer, DateTime, JSON
from datetime import datetime, timezone

class Task(Base):
    __tablename__ = "tasks"
    task_id = Column(String, primary_key=True, default=lambda: gen_id("task"))
    session_id = Column(String, nullable=False)
    status = Column(String, default="pending")  # pending | processing | completed | failed
    progress = Column(Integer, default=0)
    outputs = Column(JSON, nullable=True)       # list[OutputFile] 序列化
    error = Column(String, nullable=True)
    created_at = Column(DateTime, default=lambda: datetime.now(timezone.utc))
```

```python
# backend/models/__init__.py
from .session import Base, Session, ChatMessage, gen_id
from .file import FileRecord
from .task import Task
```

**创建数据库初始化脚本：**

```bash
# 在 Python REPL 或单独脚本中执行
poetry run python -c "
from backend.models import Base
from sqlalchemy import create_engine
engine = create_engine('sqlite:///./data.db')
Base.metadata.create_all(engine)
print('数据库建表完成')
"
```

---

### Day 3 — API 路由骨架

**任务 3.1：Session 路由**

```python
# backend/routers/session.py
from fastapi import APIRouter, HTTPException
from backend.schemas import SessionCreate, SessionInfo
from backend.models.session import Session, gen_id
from sqlalchemy import create_engine
from sqlalchemy.orm import Session as DBSession

router = APIRouter(prefix="/api/v1/sessions", tags=["sessions"])

# TODO: Day 3 只写框架，用内存 dict 代替 DB，DB 接入留到 Day 4
_sessions = {}  # 临时存储

@router.post("", status_code=201)
def create_session(req: SessionCreate):
    sid = gen_id("s")
    _sessions[sid] = {**req.model_dump(), "session_id": sid, "status": "active"}
    return _sessions[sid]

@router.get("/{session_id}")
def get_session(session_id: str):
    if session_id not in _sessions:
        raise HTTPException(404, detail="会话不存在")
    return _sessions[session_id]
```

**任务 3.2：用同样的模式完成其他路由骨架**

按照协议文档第 3 节，为每个端点创建路由文件。Day 3 用内存存储，Day 4 开始连接数据库和 AI 模块。

**每个路由文件的检查清单：**

| 路由文件 | 端点 | 状态 |
|----------|------|------|
| `session.py` | `POST /sessions`, `GET /sessions/{id}`, `GET /sessions` | □ |
| `file.py` | `POST /files/upload`, `GET /files/{id}` | □ |
| `chat.py` | `POST /sessions/{id}/chat` (SSE) | □ |
| `generate.py` | `POST /generate`, `GET /tasks/{id}/status` | □ |
| `speech.py` | `POST /speech/transcribe` | □ |
| `download.py` | `GET /download/{file_id}` | □ |

---

## Phase 2：核心开发（Day 4-12）

### 你的开发顺序（严格按此顺序，不要跳）

```
Day 4:  DB 接入（替换内存 dict → SQLAlchemy）
Day 5:  文件上传完整实现（保存 + 校验 + 返回 FileInfo）
Day 6:  对话 SSE 接入（对接 C 的 IntentAnalyzer）
Day 7:  语音识别接入（对接 C 的 SpeechTranscriber）
Day 8:  orchestrator.py 编写（对接 C 和 D 的全部函数）
Day 9:  课件生成 + 任务系统
Day 10: 下载 + 错误处理完善
Day 11-12: 与 B/C/D 逐一联调
```

### Day 6 的关键代码：SSE 对话（A ↔ C 对接点）

```python
# backend/routers/chat.py
import json
from fastapi import APIRouter
from fastapi.responses import StreamingResponse
from backend.schemas import ChatRequest, ChatEvent, MessageType
from ai.intent.analyzer import IntentAnalyzer          # ← C 写好后你直接 import

router = APIRouter(prefix="/api/v1/sessions", tags=["chat"])

# 从 config 初始化（在 main.py 的 lifespan 中完成）
intent_analyzer: IntentAnalyzer = None

def _sse_event(event: ChatEvent) -> str:
    """把 ChatEvent 序列化为 SSE data 行"""
    return f"data: {event.model_dump_json()}\n\n"

async def chat_stream(session_id: str, message: str):
    """SSE 生成器 — 逐事件推给前端"""
    messages = load_history(session_id)   # 从 DB 加载对话历史
    messages.append({"role": "user", "content": message})

    # 调 C 的意图分析器
    result = intent_analyzer.analyze(session_id, messages)

    if not result.is_complete:
        # 发送追问
        event = ChatEvent(
            event_type=MessageType.QUESTION,
            content=result.follow_up_question,
            data={"missing": result.missing_info}
        )
        yield _sse_event(event)
    else:
        # 发送确认总结
        event = ChatEvent(
            event_type=MessageType.CONFIRM,
            content=result.confirm_summary,
            data=None
        )
        yield _sse_event(event)

    yield "data: [DONE]\n\n"

@router.post("/{session_id}/chat")
async def chat(session_id: str, req: ChatRequest):
    return StreamingResponse(
        chat_stream(session_id, req.message),
        media_type="text/event-stream",
        headers={"Cache-Control": "no-cache", "X-Accel-Buffering": "no"}
    )
```

### Day 8 的关键代码：orchestrator.py（全部角色的交汇点）

> 严格按协议文档第 5 节的代码写。这是整个项目最重要的一个文件。

---

## Phase 3-4：联调与交付（Day 10-19）

### 联调顺序（与谁先调）

```
1. 先跟 C 调：对话 SSE + 意图分析  → 预期 C 的 analyzer.analyze() 能跑通
2. 再跟 C 调：语音识别            → 预期 C 的 transcriber.transcribe() 返回正确文字
3. 再跟 D 调：课件生成            → 预期 D 的 gen_ppt/gen_doc 生成文件
4. 最后跟 B 调：前后端全链路       → 预期完整流程走通
5. 全程 E 盯着你的 Swagger 和 Postman 做测试
```

### 你需要交给其他人的东西

| 交给谁 | 是什么 | 什么时候 |
|--------|--------|----------|
| 全队 | `.env` 文件 + `pyproject.toml` | Day 1 晚上 |
| C + D | `backend/schemas.py`（Pydantic 模型） | Day 2 晚上 |
| B + E | Swagger 文档 `http://localhost:8000/docs` | Day 3 晚上 |
| B + E | Postman Collection（导出 JSON） | Day 9 晚上 |
| E（归档） | 数据库 ER 图截图 + `init_db.py` | Day 19 |

### 你需要从谁那里拿到什么

| 从谁 | 是什么 | 怎么用 |
|------|--------|--------|
| C | `ai/intent/analyzer.py` 的 `IntentAnalyzer` 类 | `from ai.intent.analyzer import IntentAnalyzer` |
| C | `ai/rag/retriever.py` 的 `RAGRetriever` 类 | `from ai.rag.retriever import RAGRetriever` |
| C | `ai/fusion/fusion.py` 的 `KnowledgeFuser` 类 | `from ai.fusion.fusion import KnowledgeFuser` |
| C | `ai/speech/transcriber.py` 的 `SpeechTranscriber` 类 | `from ai.speech.transcriber import SpeechTranscriber` |
| D | `gen/pptx/generator.py` 的 `gen_ppt` 函数 | `from gen.pptx.generator import gen_ppt` |
| D | `gen/docx/generator.py` 的 `gen_doc` 函数 | `from gen.docx.generator import gen_doc` |
| D | `gen/anim/generator.py` 的 `gen_animation` 函数 | `from gen.anim.generator import gen_animation` |

> **如果 import 报错，说明对方还没写好或放错了位置。立刻找对应的人。**

---

## 今天（Day 1）立刻做这三件事

**① 初始化项目 + 安装全部依赖（贴在终端里执行）**

```bash
cd "C:\Users\kaipie\Desktop\省服务外包大赛\多模态AI教学系统"
poetry new edu-agent --flat --name backend
# 编辑 pyproject.toml，把协议文档 1.2 节的依赖粘进去
poetry install
```

**② 创建所有目录 + `.env` + `.gitignore` + `config.py` + `main.py`**

把上面 Day 1 的代码依次粘到对应文件里，启动 `python -m backend.main`，浏览器打开 `http://localhost:8000/health`，看到 `{"status":"ok"}` 就截图发群。

**③ 发 `.env` 模板给全队**

把你的 `.env` 文件（API Key 用 `sk-placeholder` 占位）发到群里，让 C 填上真实的 DeepSeek Key。

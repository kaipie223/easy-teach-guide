# 多模态AI互动式教学智能体

> 省服务外包大赛 - 智能计算 · 应用类 | 锐捷网络
> 当前阶段：Phase 1 — 环境搭建与接口定义（7月22日-24日）

## 项目目标

开发以教师教学思路为核心的"多模态AI互动式教学智能体"：
- **理解意图**：多轮对话深度理解教师教学思路，主动追问、确认
- **融合多模态参考**：解析 PDF/Word/视频/图片等参考资料
- **生成课件初稿**：自动生成 PPT + Word 教案 + 动画/互动游戏
- **迭代优化**：预览→反馈→再生成的闭环

## 已确定技术栈

| 层级 | 技术 |
|------|------|
| 前端 | Vue 3 + Vite + Element Plus |
| 后端 | FastAPI (Python 3.11) |
| 数据库 | SQLite（开发）→ PostgreSQL（上线） |
| LLM | DeepSeek-V3 API |
| 向量库 | ChromaDB |
| RAG | LangChain |
| 语音 | faster-whisper |
| PPT/Word | python-pptx / python-docx |

## 团队角色

| 姓名 | 角色 | 核心职责 |
|------|------|----------|
| 姜文杰 | 后端工程师 | API + 数据库 + 文件系统 + 系统集成（编排调度所有模块） |
| 潘卓然 | 前端工程师 | Vue3 交互界面 + 对话 + 预览 + 导出 |
| 陈澜 | AI 工程师（理解） | RAG + 意图理解 + 知识融合 + 语音 |
| 赵钰洁 | AI 工程师（生成） | PPT/Word 生成 + 多模态解析 + 动画 |
| 陶克钦 | PM/测试/数据 | 进度管理 + 测试验收 + 知识库 + 文档 |

---

## 功能模块划分（纵向切分）

> 核心思路：**不以技术层（前端/后端/AI）划分工作，而以用户可见的功能模块划分。**
> 每个模块有明确的 Owner，Owner 对该模块的**端到端交付**负责（前端→后端→AI 全链路）。

| 编号 | 模块名 | 一句话 | Owner | 涉及人员 |
|------|--------|--------|-------|---------|
| **M1** | 会话与对话引导 | 多轮对话 → AI追问 → 确认教学意图 | **陈澜** | 陈澜(意图引擎) + 姜文杰(Chat API) + 潘卓然(对话UI) |
| **M2** | 文件上传与多模态解析 | 上传PDF/Word/图片/视频 → 解析提取文本 | **赵钰洁** | 赵钰洁(解析器) + 姜文杰(上传API) + 潘卓然(上传组件) |
| **M3** | 知识库与RAG检索 | 知识库构建 → 向量化 → 语义检索 | **陈澜** | 陈澜(RAG全链路) + 陶克钦(收集资料) |
| **M4** | 语音输入 | 浏览器录音 → 语音转文字 → 填入对话 | **陈澜** | 陈澜(Whisper) + 姜文杰(语音API) + 潘卓然(录音组件) |
| **M5** | 课件智能生成 | 意图+RAG+参考 → 指令融合 → PPT+Word+动画 | **赵钰洁** | 赵钰洁(PPT/Word/动画生成) + 陈澜(知识融合) + 姜文杰(编排器) |
| **M6** | 课件预览与反馈迭代 | 在线预览 → 修改意见 → 重新生成 | **潘卓然** | 潘卓然(预览页+反馈面板) + 姜文杰(任务轮询API) |
| **M7** | 导出下载 | 课件文件打包下载 | **潘卓然** | 潘卓然(下载UI) + 姜文杰(下载API) |

### 模块依赖关系与开发顺序

```
M3 知识库 ──┐
M4 语音 ────┤
            ├──► M5 课件生成 ──► M6 预览反馈 ──► M7 导出下载
M1 对话 ────┤
M2 文件解析 ─┘

Phase 1（7/22-7/24）:   所有模块的环境搭建 + 接口定义
Phase 2（7/25-8/5）:    M1+M2+M3+M4 核心逻辑开发（并行）
Phase 3（8/1-8/12）:    M5 课件生成（依赖 M1+M2+M3 就绪）
Phase 4（8/8-8/16）:    M6 预览反馈 + M7 导出（依赖 M5 就绪）
Phase 5（8/13-8/22）:   全链路联调 + 测试 + 文档 + 演示
```

### 每个模块的接口契约

| 模块 | 输入 | 输出 | 模块间接口 |
|------|------|------|-----------|
| M1 | 用户消息文本 | IntentResult（结构化意图） | → M5 消费 |
| M2 | 上传文件（PDF/Word/视频等） | ReferenceMaterial 列表 | → M5 消费 |
| M3 | knowledge-base/ 资料 | RAGDocument 列表（检索结果） | → M5 消费 |
| M4 | 音频文件 | 文本字符串 | → M1 消费 |
| M5 | IntentResult + RAGDocument + ReferenceMaterial | .pptx + .docx + .html 文件 | → M6/M7 消费 |
| M6 | 修改意见文本 | 新 task_id（触发重新 M5） | → M5 触发 |
| M7 | file_id | 浏览器下载 | 终点 |

## 执行文档（必读）

| 文档 | 用途 |
|------|------|
| `docs/00_总执行手册_Master_Plan.md` | 架构、里程碑、工程规范 |
| `docs/01_技术协议与接口规范_Technical_Protocol.md` | **核心**：所有模块间的接口合同 |
| `docs/09_功能模块划分与开发总览.md` | **模块视图**：7 个功能模块详细定义 + 分工 + 时间线 |
| `docs/个人执行文档_Personal_Guides/` | 每人精确到文件路径的操作指令（含模块版） |

## 开发阶段

- [ ] Phase 1 — 环境搭建与接口定义（7月22日-24日，进行中）
- [ ] Phase 2 — 核心逻辑与模型联调（7月25日-8月5日）
- [ ] Phase 3 — 前端接入与联调测试（8月1日-8月12日）
- [ ] Phase 4 — 文档与交付（8月13日-8月22日）

## 工程规范（铁律）

- **命名**：极简、专业。Python `snake_case`，Vue `PascalCase` 组件
- **Git**：`dev` 主分支，功能分支 `feat/<模块>-<简述>`，PR → review → merge
- **站会**：每日 21:00，15 分钟
- **阻塞**：自查 30min → 群内求助 2h → 立即找陶克钦（PM）
- **接口变更**：先更新协议文档，再改代码

## 记忆系统

所有决策记录在 `.claude/projects/` 记忆目录。
命名：`kebab-case.md`
